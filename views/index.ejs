<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>

    <main>
      <% messages.forEach(message => { %>
      <article>
        <p><%= message.user %></p>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
      </article>
      <% }) %>
    </main>
  </body>
</html>
