<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>

    <div>
      <a href="new">Add New Message</a>
    </div>
    <hr />

    <main>
      <% messages.forEach(message => { %>
      <article>
        <p><%= message.username %></p>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
        <a href="messages/<%= message.id %>" rel="noreferrer" target="_blank">
          open
        </a>
      </article>
      <hr />
      <% }) %>
    </main>
  </body>
</html>
